make a function to catch in every input
length
special characters
different languages char
empty
mail and phone data type
spaces enters etc
avoid repeated contacts
maybe delete contacts in the future?

use strinputs stroutputs errors etc

run a command to capture exit codes / statuses 

# Run a command that may return different exit codes
some_command

# Capture the exit status
exit_status=$?

# Check different exit codes
if [ $exit_status -eq 0 ]; then
    echo "Command succeeded."
else
    echo "Command failed with exit code: $exit_status."
fi

cambiar variables de global a local
meter case en un loop en lo posible
no usar echoes anidados en manu usar contrabarras

arreglar cosas que quedan en la misma linea en case
poner tal vez una opcion ver agenda cat 
borrar contacto
borrar agenda all
